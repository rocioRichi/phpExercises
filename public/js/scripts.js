function navigate(direction) {
  // Lógica para cambiar el ejercicio
  alert(`Navegando al ejercicio ${direction > 0 ? "siguiente" : "anterior"}`);
  // Aquí puedes añadir la lógica para cambiar el contenido del ejercicio.
}
